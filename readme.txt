/// This demo is for :

  Custom SharePoint Workflow Editor sample.

/// How to install

If you use Microsoft.SharePoint.WorkflowActions.dll, you must start in SharePoint server (need SharePoint).
This uses this dll's stub code. So, this uses web service (used in SPD), so you can deploy to the remote server.
